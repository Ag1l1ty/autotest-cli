<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Code Doctor - {{ project.name }}</title>
    <style>
        :root {
            --bg: #0d1117;
            --bg-secondary: #161b22;
            --bg-card: #21262d;
            --text: #c9d1d9;
            --text-muted: #8b949e;
            --border: #30363d;
            --green: #3fb950;
            --green-dim: rgba(63, 185, 80, 0.15);
            --red: #f85149;
            --yellow: #d29922;
            --orange: #db6d28;
            --blue: #58a6ff;
            --purple: #bc8cff;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;
            background: var(--bg);
            color: var(--text);
            line-height: 1.6;
        }
        .container { max-width: 1200px; margin: 0 auto; padding: 24px; }
        header {
            background: var(--bg-secondary);
            border-bottom: 1px solid var(--border);
            padding: 24px 0;
            margin-bottom: 24px;
        }
        header h1 { font-size: 24px; font-weight: 600; }
        header .meta { color: var(--text-muted); font-size: 14px; margin-top: 4px; }
        .score-badge {
            display: inline-block;
            font-size: 48px;
            font-weight: 700;
            padding: 16px 24px;
            border-radius: 12px;
            text-align: center;
        }
        .score-healthy { background: rgba(63, 185, 80, 0.15); color: var(--green); }
        .score-moderate { background: rgba(210, 153, 34, 0.15); color: var(--yellow); }
        .score-at-risk { background: rgba(248, 81, 73, 0.15); color: var(--red); }
        .score-critical { background: rgba(248, 81, 73, 0.25); color: var(--red); }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px; margin-bottom: 24px; }
        .card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 20px;
        }
        .card h3 { font-size: 14px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 12px; }
        .card .value { font-size: 28px; font-weight: 600; }
        .section { margin-bottom: 32px; }
        .section h2 {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 16px;
            padding-bottom: 8px;
            border-bottom: 1px solid var(--border);
        }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 10px 12px; text-align: left; border-bottom: 1px solid var(--border); }
        th { color: var(--text-muted); font-weight: 500; font-size: 13px; text-transform: uppercase; }
        .badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
        }
        .badge-critical { background: rgba(248, 81, 73, 0.2); color: var(--red); }
        .badge-warning { background: rgba(210, 153, 34, 0.2); color: var(--yellow); }
        .badge-info { background: rgba(88, 166, 255, 0.2); color: var(--blue); }
        .badge-lang { background: rgba(88, 166, 255, 0.2); color: var(--blue); }
        .badge-category { background: rgba(188, 140, 255, 0.2); color: var(--purple); font-size: 11px; }
        details { margin-bottom: 8px; }
        details summary {
            cursor: pointer;
            padding: 10px 12px;
            border-radius: 4px;
            background: var(--bg-secondary);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        details summary:hover { background: var(--bg-card); }
        .finding-body {
            padding: 16px;
            background: var(--bg-card);
            border-radius: 0 0 4px 4px;
            margin-top: 2px;
        }
        pre {
            background: var(--bg);
            padding: 12px;
            border-radius: 4px;
            overflow-x: auto;
            font-size: 13px;
            margin-top: 8px;
        }
        .fix-box {
            border-left: 3px solid var(--green);
            padding: 12px 16px;
            background: rgba(63, 185, 80, 0.05);
            border-radius: 0 4px 4px 0;
            margin-top: 12px;
        }
        .fix-box h4 { color: var(--green); margin-bottom: 8px; font-size: 14px; }
        .code-diff { margin-top: 8px; }
        .code-before { border-left: 3px solid var(--red); padding-left: 8px; margin-bottom: 4px; }
        .code-after { border-left: 3px solid var(--green); padding-left: 8px; }
        .action-item {
            padding: 10px 16px;
            margin-bottom: 8px;
            border-left: 3px solid var(--blue);
            background: rgba(88, 166, 255, 0.05);
            border-radius: 0 4px 4px 0;
        }
        .action-item a { color: var(--text); text-decoration: none; }
        .action-item a:hover { color: var(--blue); }
        .action-number {
            display: inline-block;
            width: 24px;
            height: 24px;
            border-radius: 50%;
            background: var(--blue);
            color: var(--bg);
            text-align: center;
            line-height: 24px;
            font-weight: 600;
            font-size: 13px;
            margin-right: 8px;
        }
        .copy-btn {
            display: inline-block;
            padding: 3px 10px;
            font-size: 11px;
            color: var(--text-muted);
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 4px;
            cursor: pointer;
            margin-top: 6px;
            transition: all 0.2s;
        }
        .copy-btn:hover { background: var(--bg-card); color: var(--text); border-color: var(--green); }
        .copy-btn.copied { color: var(--green); border-color: var(--green); }
        /* Highlights */
        .highlights-grid { display: flex; flex-wrap: wrap; gap: 12px; margin-bottom: 24px; }
        .highlight-card {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 16px;
            background: rgba(63, 185, 80, 0.08);
            border: 1px solid rgba(63, 185, 80, 0.2);
            border-radius: 8px;
            font-size: 14px;
            color: var(--green);
        }
        .highlight-icon { font-size: 16px; }
        /* Urgency gradient for warnings */
        .urgency-extreme summary { border-left: 3px solid var(--red) !important; background: rgba(248, 81, 73, 0.08); }
        .urgency-high summary { border-left: 3px solid var(--orange) !important; background: rgba(219, 109, 40, 0.06); }
        .urgency-moderate summary { border-left: 3px solid var(--yellow); }
        /* Filter banner */
        .filter-banner {
            background: rgba(88, 166, 255, 0.08);
            border: 1px solid rgba(88, 166, 255, 0.2);
            border-left: 3px solid var(--blue);
            border-radius: 0 8px 8px 0;
            padding: 12px 16px;
            margin-bottom: 24px;
            font-size: 14px;
            color: var(--text-muted);
        }
        .filter-banner code {
            background: var(--bg-card);
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 12px;
        }
        footer {
            text-align: center;
            color: var(--text-muted);
            font-size: 12px;
            padding: 24px 0;
            border-top: 1px solid var(--border);
            margin-top: 48px;
        }
        footer code {
            background: var(--bg-card);
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 11px;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>Code Doctor: {{ project.name }}</h1>
            <div class="meta">
                <span style="font-family: monospace; background: var(--bg-card); padding: 2px 8px; border-radius: 4px;">{{ report_id }}</span>
                &nbsp;|&nbsp; Generated {{ generated_at }} &nbsp;|&nbsp; Code Doctor v{{ version }}
            </div>
        </div>
    </header>

    <div class="container">
        {% if diagnosis %}
        <!-- Health Score -->
        <div class="section" style="text-align: center; margin-bottom: 32px;">
            <div class="score-badge score-{{ diagnosis.health_label }}">
                {{ diagnosis.health_score | round(0) | int }}/100
            </div>
            <div style="margin-top: 8px; color: var(--text-muted);">
                Health Score: <strong>{{ diagnosis.health_label | upper }}</strong>
            </div>
            {% if score_breakdown %}
            <div style="margin-top: 6px; font-size: 12px; color: var(--text-muted);">
                100 {% for label, penalty in score_breakdown %}&minus;{{ penalty | round(1) }} <span style="opacity: 0.7;">({{ label }})</span>{% if not loop.last %} {% endif %}{% endfor %} = {{ diagnosis.health_score | round(0) | int }}
            </div>
            {% endif %}
        </div>

        <!-- Overview Cards -->
        <div class="grid">
            <div class="card">
                <h3>Critical</h3>
                <div class="value" style="color: var(--red);">{{ diagnosis.critical_count }}</div>
            </div>
            <div class="card">
                <h3>Warnings</h3>
                <div class="value" style="color: var(--yellow);">{{ diagnosis.warning_count }}</div>
            </div>
            <div class="card">
                <h3>Info</h3>
                <div class="value" style="color: var(--blue);">{{ diagnosis.info_count }}</div>
            </div>
            <div class="card">
                <h3>Functions</h3>
                <div class="value">{{ analysis.total_functions }}</div>
                {% if untested_total > 0 %}
                <div style="color: var(--text-muted); margin-top: 4px; font-size: 13px;">
                    {{ untested_total }} untested
                </div>
                <div style="color: var(--text-muted); font-size: 11px; margin-top: 2px;">
                    {% for name in untested_names %}{{ name }}(){% if not loop.last %}, {% endif %}{% endfor %}{% if untested_total > 5 %} y {{ untested_total - 5 }} mas{% endif %}
                </div>
                {% endif %}
            </div>
            <div class="card">
                <h3>Languages</h3>
                <div class="value">{{ project.languages | length }}</div>
                <div style="margin-top: 8px;">
                    {% for lang in project.languages %}
                    <span class="badge badge-lang">{{ lang.language.value }}</span>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- Highlights (positive metrics) -->
        {% if highlights %}
        <div class="highlights-grid">
            {% for icon, text in highlights %}
            <div class="highlight-card">
                <span class="highlight-icon">{% if icon == "check" %}&#10003;{% elif icon == "shield" %}&#9899;{% elif icon == "lock" %}&#128274;{% endif %}</span>
                {{ text }}
            </div>
            {% endfor %}
        </div>
        {% endif %}

        <!-- Top Actions (executive summary with links) -->
        {% if top_actions %}
        <div class="section">
            <h2>Top Acciones Prioritarias</h2>
            {% for finding in top_actions %}
            <div class="action-item">
                <a href="#finding-{{ finding.id }}">
                    <span class="action-number">{{ loop.index }}</span>
                    {% if cc_values.get(finding.id, 0) >= 20 %}
                    <span style="display: inline-block; padding: 1px 6px; border-radius: 3px; font-size: 10px; font-weight: 600; background: rgba(248, 81, 73, 0.2); color: var(--red); margin-right: 6px;">ALTO IMPACTO</span>
                    {% elif cc_values.get(finding.id, 0) >= 10 %}
                    <span style="display: inline-block; padding: 1px 6px; border-radius: 3px; font-size: 10px; font-weight: 600; background: rgba(210, 153, 34, 0.2); color: var(--yellow); margin-right: 6px;">MEDIO</span>
                    {% endif %}
                    <strong>{{ finding.suggested_fix.description }}</strong>
                    {% if finding.file_path and finding.line_start %}
                    <span style="color: var(--text-muted); font-size: 13px;">
                        &mdash; {{ finding.file_path }}:{{ finding.line_start }}
                    </span>
                    {% endif %}
                </a>
            </div>
            {% endfor %}
        </div>
        {% endif %}

        <!-- Findings by Severity -->
        {% if criticals %}
        <div class="section">
            <h2 style="color: var(--red);">Critical ({{ criticals | length }})</h2>
            {% if critical_summary %}
            <div style="color: var(--text-muted); font-size: 13px; margin: -8px 0 12px 0;">{{ critical_summary }}</div>
            {% endif %}
            {% for finding in criticals %}
            <details id="finding-{{ finding.id }}" {% if loop.index <= 5 %}open{% endif %}
                class="{% if cc_values.get(finding.id, 0) >= 25 %}urgency-extreme{% elif cc_values.get(finding.id, 0) >= 15 %}urgency-high{% endif %}">
                <summary style="{% if cc_values.get(finding.id, 0) < 15 %}border-left: 3px solid var(--red);{% endif %}">
                    <span class="badge badge-critical">{{ finding.id }}</span>
                    <span class="badge badge-category">{{ finding.category.value }}</span>
                    {% if finding.file_path %}
                    <span style="font-family: monospace; font-size: 13px;">{{ finding.file_path }}{% if finding.line_start %}:{{ finding.line_start }}{% endif %}</span>
                    {% endif %}
                    <span style="flex: 1;">{{ finding.title }}</span>
                </summary>
                <div class="finding-body">
                    <p>{{ finding.description }}</p>
                    {% if finding.suggested_fix %}
                    <div class="fix-box">
                        <h4>Fix: {{ finding.suggested_fix.description }}</h4>
                        {% if finding.suggested_fix.code_before %}
                        <div class="code-diff">
                            <div class="code-before"><pre>{{ finding.suggested_fix.code_before }}</pre></div>
                            {% if finding.suggested_fix.code_after %}
                            <div class="code-after"><pre>{{ finding.suggested_fix.code_after }}</pre></div>
                            <button class="copy-btn" onclick="copyFix(this)">Copiar fix</button>
                            {% endif %}
                        </div>
                        {% endif %}
                        {% if finding.suggested_fix.explanation %}
                        <p style="color: var(--text-muted); margin-top: 8px; font-size: 13px;">{{ finding.suggested_fix.explanation }}</p>
                        {% endif %}
                    </div>
                    {% endif %}
                </div>
            </details>
            {% endfor %}
        </div>
        {% endif %}

        {% if warnings %}
        <div class="section">
            <h2 style="color: var(--yellow);">Warnings ({{ warnings | length }})</h2>
            {% if warning_summary %}
            <div style="color: var(--text-muted); font-size: 13px; margin: -8px 0 12px 0;">{{ warning_summary }}</div>
            {% endif %}
            {% for finding in warnings %}
            <details id="finding-{{ finding.id }}" {% if loop.index <= 3 %}open{% endif %}
                class="{% if cc_values.get(finding.id, 0) >= 25 %}urgency-extreme{% elif cc_values.get(finding.id, 0) >= 15 %}urgency-high{% endif %}">
                <summary style="{% if cc_values.get(finding.id, 0) < 15 %}border-left: 3px solid var(--yellow);{% endif %}">
                    <span class="badge badge-warning">{{ finding.id }}</span>
                    <span class="badge badge-category">{{ finding.category.value }}</span>
                    {% if finding.file_path %}
                    <span style="font-family: monospace; font-size: 13px;">{{ finding.file_path }}{% if finding.line_start %}:{{ finding.line_start }}{% endif %}</span>
                    {% endif %}
                    <span style="flex: 1;">{{ finding.title }}</span>
                </summary>
                <div class="finding-body">
                    <p>{{ finding.description }}</p>
                    {% if finding.suggested_fix %}
                    <div class="fix-box">
                        <h4>Fix: {{ finding.suggested_fix.description }}</h4>
                        {% if finding.suggested_fix.code_before %}
                        <div class="code-diff">
                            <div class="code-before"><pre>{{ finding.suggested_fix.code_before }}</pre></div>
                            {% if finding.suggested_fix.code_after %}
                            <div class="code-after"><pre>{{ finding.suggested_fix.code_after }}</pre></div>
                            <button class="copy-btn" onclick="copyFix(this)">Copiar fix</button>
                            {% endif %}
                        </div>
                        {% endif %}
                        {% if finding.suggested_fix.explanation %}
                        <p style="color: var(--text-muted); margin-top: 8px; font-size: 13px;">{{ finding.suggested_fix.explanation }}</p>
                        {% endif %}
                    </div>
                    {% endif %}
                </div>
            </details>
            {% endfor %}
        </div>
        {% endif %}

        {% if infos %}
        <div class="section">
            <h2 style="color: var(--blue);">Info ({{ infos | length }})</h2>
            {% if info_summary %}
            <div style="color: var(--text-muted); font-size: 13px; margin: -8px 0 12px 0;">{{ info_summary }}</div>
            {% endif %}
            {% for finding in infos %}
            <details id="finding-{{ finding.id }}">
                <summary style="border-left: 3px solid var(--blue);">
                    <span class="badge badge-info">{{ finding.id }}</span>
                    <span class="badge badge-category">{{ finding.category.value }}</span>
                    {% if finding.file_path %}
                    <span style="font-family: monospace; font-size: 13px;">{{ finding.file_path }}{% if finding.line_start %}:{{ finding.line_start }}{% endif %}</span>
                    {% endif %}
                    <span style="flex: 1;">{{ finding.title }}</span>
                </summary>
                <div class="finding-body">
                    <p>{{ finding.description }}</p>
                    {% if finding.suggested_fix %}
                    <div class="fix-box">
                        <h4>Fix: {{ finding.suggested_fix.description }}</h4>
                        {% if finding.suggested_fix.code_before %}
                        <div class="code-diff">
                            <div class="code-before"><pre>{{ finding.suggested_fix.code_before }}</pre></div>
                            {% if finding.suggested_fix.code_after %}
                            <div class="code-after"><pre>{{ finding.suggested_fix.code_after }}</pre></div>
                            <button class="copy-btn" onclick="copyFix(this)">Copiar fix</button>
                            {% endif %}
                        </div>
                        {% endif %}
                        {% if finding.suggested_fix.explanation %}
                        <p style="color: var(--text-muted); margin-top: 8px; font-size: 13px;">{{ finding.suggested_fix.explanation }}</p>
                        {% endif %}
                    </div>
                    {% endif %}
                </div>
            </details>
            {% endfor %}
        </div>
        {% endif %}

        <!-- Hidden findings banner -->
        {% if hidden_total > 0 %}
        <div class="filter-banner">
            {{ hidden_total }} hallazgo(s) oculto(s) por filtro de severidad.
            Ejecutar con <code>--severity critical,warning,info</code> para verlos.
        </div>
        {% endif %}

        <!-- Code Analysis Summary -->
        <div class="section">
            <h2>Code Analysis</h2>
            <div class="card">
                <table>
                    <tr>
                        <td>Total Functions</td>
                        <td><strong>{{ analysis.total_functions }}</strong></td>
                    </tr>
                    <tr>
                        <td>Tested Functions</td>
                        <td><strong>{{ analysis.tested_function_count }}</strong></td>
                    </tr>
                    <tr>
                        <td>Average Complexity</td>
                        <td><strong>{{ analysis.avg_complexity | round(1) }}</strong></td>
                    </tr>
                    <tr>
                        <td>Total Lines of Code</td>
                        <td><strong>{{ analysis.total_loc }}</strong></td>
                    </tr>
                    <tr>
                        <td>Dead Code Functions</td>
                        <td><strong>{{ analysis.dead_code_functions | length }}</strong></td>
                    </tr>
                    <tr>
                        <td>High Coupling Modules</td>
                        <td><strong>{{ analysis.coupling_issues | length }}</strong></td>
                    </tr>
                    {% if analysis.estimated_coverage > 0 %}
                    <tr>
                        <td>Estimated Coverage</td>
                        <td><strong>{{ analysis.estimated_coverage | round(1) }}%</strong></td>
                    </tr>
                    {% endif %}
                </table>
                <!-- CC Legend -->
                <details style="margin-top: 12px;">
                    <summary style="font-size: 13px; color: var(--text-muted); cursor: pointer; background: transparent; padding: 4px 0;">
                        &#9432; Que es CC (Complejidad Ciclomatica)?
                    </summary>
                    <div style="padding: 12px; font-size: 13px; color: var(--text-muted); background: var(--bg); border-radius: 4px; margin-top: 8px;">
                        <strong>CC</strong> mide el numero de caminos independientes en el codigo (ramas if/else, loops, try/catch).
                        <div style="margin-top: 8px; display: flex; gap: 16px; flex-wrap: wrap;">
                            <span>&#9679; <strong>1-10</strong> Normal</span>
                            <span style="color: var(--yellow);">&#9679; <strong>11-20</strong> Alta (dificil de testear)</span>
                            <span style="color: var(--orange);">&#9679; <strong>21-50</strong> Muy alta</span>
                            <span style="color: var(--red);">&#9679; <strong>&gt;50</strong> Critica</span>
                        </div>
                    </div>
                </details>
            </div>
        </div>

        {% else %}
        <!-- No diagnosis data -->
        <div class="section" style="text-align: center; padding: 48px;">
            <p style="color: var(--text-muted);">No diagnosis data available.</p>
        </div>
        {% endif %}
    </div>

    <footer>
        <div class="container">
            {% if fixable_count > 0 %}
            <p style="margin-bottom: 8px;">
                Ejecutar <code>autotest diagnose --fix</code> para aplicar {{ fixable_count }} correccione(s) automaticamente.
            </p>
            {% endif %}
            {% if hidden_total > 0 %}
            <p style="margin-bottom: 8px;">
                {{ hidden_total }} hallazgo(s) oculto(s) &mdash; usar <code>--severity critical,warning,info</code> para verlos.
            </p>
            {% endif %}
            <p>
                Generated by Code Doctor v{{ version }} &nbsp;|&nbsp; Report ID: <code>{{ report_id }}</code>
            </p>
        </div>
    </footer>
    <script>
    function copyFix(btn) {
        var codeAfter = btn.previousElementSibling;
        var pre = codeAfter.querySelector('pre');
        if (!pre) return;
        var text = pre.textContent;
        navigator.clipboard.writeText(text).then(function() {
            btn.textContent = 'Copiado!';
            btn.classList.add('copied');
            setTimeout(function() {
                btn.textContent = 'Copiar fix';
                btn.classList.remove('copied');
            }, 2000);
        });
    }
    </script>
</body>
</html>
